window.addEventListener('scroll',()=>{
    console.log("Scrolled");
    const scrolld = window.scrollY;
    const add = document.get 
    console.log(scrolld);
    if(scrolld>82){

    }
})

var typed = new Typed(".typing",{
    strings: ["Web Developer", "Freelancer"],
    typeSpeed: 100,
    backSpeed: 60,
    loop: true
});
console.log("hello world i m testing");

var typed = new Typed(".typin",{
    strings: ["a Web Developer", "a Freelancer"],
    typeSpeed: 100,
    backSpeed: 60,
    loop: true
});